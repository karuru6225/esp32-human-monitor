services:
  backend:
    build: ./backend
    ports:
      - "8765:8765"
    devices:
      - "/dev/ttyACM0:/dev/ttyACM0"
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
    restart: unless-stopped
