# 引き継ぎメモ（2026-08-30時点）

このプロジェクトのこれまでの経緯・検証結果・現状の判断をまとめたもの。
README.mdが「プロジェクト概要」だとすると、こちらは「途中経過の詳細と、次に何をすべきか」の記録。

## 経緯（なぜこの方向に進んでいるか）

1. Home Assistantで「いつ何をしていたか」の活動ログ（就寝中/作業中/休憩中/外出中、将来的にはもっと細かく）を可視化したい、というのが元々のゴール
2. 既存のモーションセンサーは玄関付近にしかなく、リビングでの在宅勤務中の在室・静止状態を検知できない、という制約が判明
3. mmWaveレーダー（Aqara FP2やSeeed XIAOシリーズ）を検討したが、技適（技術基準適合証明）の確認・取得の手間が課題として浮上
4. 代替として、既に技適済みのWiFi無線をそのまま使う「WiFi CSI（Channel State Information）センシング」を検証する方針にした。専用レーダーHWの購入・技適確認が不要なのが利点
5. 実装方式はESPHome + `espectre`外部コンポーネントも検討したが、コードレベルの細かい制御がしたいという要望から、**PlatformIO + Arduino framework**での自作ファームウェアを本命ルートとして選択
6. **重要な制約**：この検証段階ではMQTT/Home Assistant連携は行わない。まずはシリアルモニタで生の数値を見て、そもそもCSIが人の動き・在室状態に反応するかどうかだけを確認する

## 現在のハードウェア状況

- **car-iot開発機**（ESP32-S3、自作基板・PCBアンテナ、開発用）→ 検証済み。信号品質面で市販品より明確に劣ることが判明（詳細は下記2026-08-30の検証結果）
- **Seeed Studio XIAO ESP32C3**（市販品、外付けアンテナ）→ 2026-08-30に追加投入。`platformio.ini`に`env:xiao-c3`（`board = seeed_xiao_esp32c3`）として追加済み、ビルド・書き込み・動作確認済み
- **M5AtomS3 Lite**（ESP32-S3、市販品・外付けアンテナ、本来はSesame5⇔HA連携の本番機）→ 2026-08-30の検証で一時的にCSIテスト用ファーム（`env:esp32-s3`）を書き込み済み。**現在Sesame5⇔HA中継の本番機能は停止中**。検証が一段落したら元のファームに書き戻すこと（元プロジェクトの場所はユーザー側で把握、このリポジトリには含まれない）
- 2台以上での構成（ゾーン分離）は、1台での反応がある程度わかってから、という方針で意図的に後回しにしていたが、2026-08-30に3台を同時に使った機器間比較検証を実施済み（下記参照）

## ファームウェアの現状（`src/main.cpp`）

PlatformIO + Arduino framework、`esp_wifi_set_csi_rx_cb`でCSIコールバックを受け、以下の3つの指標をシリアル出力するだけの単体検証用コード：

- `rssi`：受信パケットの信号強度（`info->rx_ctrl.rssi`）。近い/遠いの粗い判定に一番安定して使える
- `avg_amp`：CSIバッファの生バイトを空間方向（サブキャリア間）に絶対値平均したもの。I/Qが交互に並んだ生データをそのまま平均している粗い指標で、動きへの感度は低め
- `diff_score`：前フレームとの差分の絶対値平均（時間方向の変化＝動きの指標として追加）。歩く・体を動かすといった能動的な動作にスパイクとして反応する
- `filt=採用数/受信総数`：CSIコールバックの送信元MACが接続中APのBSSIDと一致するものだけを採用するフィルタを追加した際の採用率（詳細は後述）

**解決済み**：WiFi認証情報は`src/secrets.h`（`.gitignore`済み）に分離した。`main.cpp`は`secrets.h`をincludeして`WIFI_SSID`/`WIFI_PASSWORD`マクロを参照する形。テンプレートとして`src/secrets.h.example`をコミット対象に置いてある。新しい環境でビルドする際は`secrets.h.example`を`secrets.h`にコピーして値を入れること。

## これまでの検証結果まとめ

| # | 条件 | ログファイル | サンプル数 | 主な所見 |
|---|------|-------------|-----------|----------|
| 1 | 近くに座る→歩いて離れる→戻る（1回目） | `logs/device-monitor-260830-074808.log` | 334 | RSSIが離れると明確に下がり、戻ると回復。再現性のある綺麗な信号 |
| 2 | 同上（2回目、再現性確認） | `logs/device-monitor-260830-075044.log` | 263 | 1回目とほぼ同じ傾向を再現 |
| 3 | 椅子に座っている（近い） | `logs/device-monitor-260830-075648.log` | 624 | rssi std=1.99, avg_amp std=2.63, diff_score mean=13.63 |
| 4 | 椅子近くのベッドで寝ている（近い） | `logs/device-monitor-260830-075738.log` | 547 | rssi std=1.19, avg_amp std=1.25, diff_score mean=11.13 |
| 5 | ESP32から離れた状態 | `logs/device-monitor-260830-075814.log` | 377 | rssi std=0.95, avg_amp std=0.65, diff_score mean=11.00 |
| 6 | （フィルタ追加前の追加ログ、未分析） | `logs/device-monitor-260830-080833.log` | 2020 | `filt=`フィールドなし＝MACフィルタ実装前の記録 |
| 7 | 椅子に座っている（MACフィルタ適用後） | `logs/device-monitor-260830-081303.log` | 1450+ | `filt=`が最初から最後まで100%（フィルタが一度も何かを除外しなかった） |
| 8 | S3(車iot・椅子付近, AP~1m)⇔C3(XIAO, ベッド頭付近, AP~2.5-3m) 同時記録、移動あり（11:06:30離席→玄関方向、11:09:25椅子に復帰） | `logs/device-monitor-s3-260830-110525.log`, `logs/device-monitor-c3-260830-110525.log` | S3:5877, C3:5976 | S3のrssiは今回の移動にほぼ無反応（過去の#1,2と食い違う。歩行経路がS3からの距離をあまり変えなかった可能性）。一方C3側は復帰の瞬間から+7〜8dB・30〜40秒持続するrssi上昇を観測（椅子から離れた場所なのに反応。部屋全体のマルチパス変化が波及した可能性、ただし1試行のみで要再現確認）。diff_scoreの3σスパイクはどちらの機器・イベントでもほぼ検出できず |
| 9 | S3(車iot)⇔C3(XIAO) 同一地点に並べて静止、機器差の切り分け用（動きなし） | `logs/device-monitor-s3-260830-111551-sameloc.log`, `logs/device-monitor-c3-260830-111551-sameloc.log` | S3:5873, C3:5977 | 同一地点でもrssiに約12dBの差（S3:-53.72 vs C3:-41.76）。avg_amp std（S3:1.76 vs C3:0.35）、静止時にもかかわらずdiff_scoreの3σスパイクが発生する率（S3:11/5873=0.19% vs C3:0/5977=0%）ともS3が明確に悪い。車iot機の自作PCBトレースアンテナが原因という仮説 |
| 10 | M5AtomS3 Lite(市販・外付けアンテナ)⇔C3(XIAO, 市販・外付けアンテナ) 同一地点に並べて静止 | `logs/device-monitor-atoms3-260830-112735-sameloc.log`, `logs/device-monitor-c3-260830-112735-sameloc.log` | AtomS3:5872, C3:5975 | rssi差が2.3dBまで縮小（AtomS3:-40.29 vs C3:-42.55）、diff_score平均もほぼ同一（11.34 vs 11.32）、3σスパイク率もS3:0.07%/C3:0.13%と同程度。#9で見えた12dB差は**チップ（ESP32-S3 vs C3）の差ではなく、アンテナ実装（自作PCB vs 外付け）の差**だったと確認できた |
| 11 | AtomS3(椅子付近)⇔C3(XIAO, ベッド頭付近) 実運用ゾーン配置、離席→玄関方向→復帰を2回（11:45:15/11:46:17, 11:47:05/11:47:50） | `logs/device-monitor-atoms3-260830-114426-zone.log`, `logs/device-monitor-c3-260830-114426-zone.log` | AtomS3:5881, C3:5983 | クリーンな機体だと#8よりはっきり反応が出た。**C3（遠いノード）は離席中ずっとrssiが7〜8dB下がり復帰で回復**という持続的シフトを2回とも再現。**AtomS3（近いノード）は離席中ほぼ無反応、復帰の瞬間だけ一時的にrssiが落ちる**（着席動作による近接場の乱れ、数秒で回復）。同じ移動イベントでも近いノードと遠いノードで反応の質的パターンが異なる。diff_scoreは終始ノイズの帯のようで、このイベントを判別する材料にはならなかった。グラフ：`docs/images/zone_test_260830_leave_return.png` |
| 12 | AtomS3(椅子付近)⇔C3(XIAO, ベッド頭付近) 椅子→ベッド→椅子を2回（12:05:05/12:06:20, 12:07:05/12:08:24） | `logs/device-monitor-atoms3-260830-120424-tobed.log`, `logs/device-monitor-c3-260830-120424-tobed.log` | AtomS3:5874, C3:5977 | #11とは逆に、**人がC3(ベッド)に近づく**移動。C3のrssiは1回目-48〜-53、2回目-58〜-62と、**近接時ほど大きく・かつ一定しない**下降を見せた（#11の在室/不在の一定した下げ幅とは異質。近接場での体遮蔽が支配的になった可能性）。AtomS3(椅子から離れている間)はむしろ平坦で安定、椅子に戻る瞬間だけ一過性の乱れ（今回は上振れ、#11では下振れで方向は逆だが「戻る瞬間だけ乱れる」点は共通）。**diff_scoreは今回、AtomS3側にt=130〜155付近で明確なスパイクの帯が出た**（#11では終始ノイズに埋もれて見えなかった）。グラフ：`docs/images/zone_test_260830_tobed.png` |

### 得られた知見

- **粗い位置変化（歩いて近づく/離れる）**：RSSIの平均値シフトだけで十分検出できる。#1, #2で再現性を確認済み
- **能動的な動作（座って作業中の体動）**：`diff_score`の散発的なスパイクとして拾える。#3では離れた状態の3σを超えるスパイクが624サンプル中18回（2.9%）発生していたが、#4（寝ている）・#5（離れた）では0回
- **静止していても在室/不在を区別できるか（当初の本命課題）**：#3〜#5で「座っている > 寝ている > 離れた」という綺麗な段階的パターンがrssi/avg_amp両方の標準偏差に現れた。特にavg_ampの標準偏差は寝ている(1.25)が離れた(0.65)の約2倍あり、じっとしていても在室by在/不在の違いが信号の"揺らぎの大きさ"に出ている可能性がある。**当初のPIRセンサーでは検知できない「静止した在室状態」を拾えるかもしれない、という意味で一番の収穫**
- **MACフィルタの実験結果（#7）**：CSIコールバックの送信元MACをAPのBSSIDに絞るフィルタを追加したが、採用率は常に100%だった。つまり「同一チャンネル上の他端末の通信を誤って拾っていた」という仮説は誤りで、STAモードのWiFiドライバは元々AP由来のフレームしかCSIコールバックに渡していなかったと考えられる。これにより、残っているノイズ（ルーターの自動レート制御・再送・ビームフォーミングなど）はAP側の挙動に起因するもので、**ESP32側だけでは制御できない**ことが裏付けられた
- **チップ差 vs アンテナ実装差（#8〜#10、2026-08-30）**：car-iot機（自作PCBアンテナ）とXIAO C3（市販・外付けアンテナ）を同一地点に並べても約12dBのrssi差が出た（#9）。しかしcar-iot機の代わりに市販品のM5AtomS3 Lite（同じくESP32-S3だが外付けアンテナ）で比較し直すと差はわずか2.3dBまで縮小し、diff_scoreの平均・3σ誤検知率もほぼ同水準になった（#10）。**ESP32-S3とESP32-C3自体のCSIセンシング特性に大きな差はなく、car-iot機の信号品質の悪さはアンテナ実装（自作PCBトレース）に起因する**と判断できる
- **近いノードと遠いノードで移動イベントへの反応パターンが質的に違う（#11、2026-08-30）**：クリーンな機体（AtomS3・XIAO C3）で椅子からの離席→復帰を2回行ったところ、**遠いノード（C3、ベッド頭付近）は離席中ずっとrssiが7〜8dB下がる持続的なレベルシフト**を示し、**近いノード（AtomS3、椅子付近）は復帰の瞬間だけ数秒間rssiが落ちる一過性の反応**を示した。2サイクルとも同じパターンが再現している。`diff_score`は終始高いノイズの帯になっていて、この程度の歩行イベントを判別する材料としては機能していない（グラフで一目瞭然）

  ![離席復帰時のrssi/diff_score波形（AtomS3 vs XIAO C3）](images/zone_test_260830_leave_return.png)

- **「離れた場所への移動」と「近づく移動」で反応の質がさらに違う（#12、2026-08-30）**：#11は椅子からベッドと無関係な玄関方向への移動だったが、#12では逆に**人がC3(ベッド)自体に近づく**移動を試した。結果、C3のrssiは#11のような一定した下げ幅ではなく、**近接時ほど大きく・かつ試行ごとにばらつく**下降（1回目-48〜-53、2回目-58〜-62）を見せた。人体がアンテナのごく近くにあることによる**近接場の遮蔽効果**が、単純な在室/不在の差より支配的になっていると考えられる。また`diff_score`も#11では終始ノイズに埋もれていたが、#12ではAtomS3側にt=130〜155付近で明確なスパイクの帯が出た。**`diff_score`はノードの近くでの動きにしか反応せず、`rssi`は近接時の体遮蔽と遠隔時の在室/不在とで異なる質の変化を示す**、という構造が見えてきた

  ![椅子↔ベッド移動時のrssi/diff_score波形（AtomS3 vs XIAO C3）](images/zone_test_260830_tobed.png)
- **静止時でも`diff_score`の3σスパイクは偶発的に発生する（#9, #10）**：動きが全くない状態でも、車iot機で0.19%、M5AtomS3 Liteで0.07%、XIAO C3で0〜0.13%の割合で3σを超えるスパイクが発生した。将来`diff_score`を動き検知の閾値判定に使う場合、この誤検知ベースラインを踏まえた閾値設計が必要
- **1台のノードでも、その場所から離れた場所での人の動きに反応することがある（#8、要再現確認）**：椅子の近くから離れた場所（ベッド頭付近）に置いたXIAO C3が、椅子への「復帰」イベントの瞬間から+7〜8dB・30秒以上持続するrssi上昇を示した。物理的に近接していない場所での行動が、部屋全体のマルチパスを通じて別ノードのCSI/rssiに影響する可能性を示唆するが、1試行のみでノイズとの区別がついていない

### 未解決・注意点

- 各条件とも試行回数が1回ずつで、録画時間もバラバラ（377〜1450+サンプル）。繰り返し検証・時間を揃えた比較はまだできていない
- #3（座っている）のRSSIグラフには、サンプル200付近で-53dBmから-58dBmへの明確な段差がある。記録中に無意識に姿勢を変えた可能性が高いが未確認
- `avg_amp`の標準偏差は#3〜#5と#7で大きく異なる（2.63 vs 0.35）。フィルタは無関係（100%通過のため）なので、椅子の位置・向き・APが選んだ変調方式など、他の条件差による可能性が高い。原因は特定できていない
- `main.cpp`のコメントにある通り、`wifi_csi_info_t`のフィールド名（`info->mac`等）はArduinoコア/esp-idfのバージョンで変わる可能性があり、このセッションからは実機ビルドができないため、コンパイル確認はユーザー側の環境で行う必要がある

## 次のアクション候補（優先度つき）

0. **【要対応】M5AtomS3 Liteへの本番ファーム書き戻し**：2026-08-30の検証で一時的にCSIテスト用ファームを書き込んだままになっている。Sesame5⇔HA連携の中継機能が止まっているので、検証が一段落し次第、元のファームに書き戻すこと
1. ~~XIAO C3が示したrssi急上昇（#8）の再現性確認~~ → **解決**：#11でクリーンな機体（AtomS3・XIAO C3）を使って2サイクル再現。遠いノードが在室/不在で持続的にrssiシフトすることを確認済み
1b. **#12で見えた「近接時の体遮蔽によるrssi低下のばらつき」の原因特定**：ベッドに近づいた際のC3のrssi低下幅が1回目(-48〜-53)と2回目(-58〜-62)で大きく違った。座る/横になる等の姿勢差が原因か、複数回試して切り分ける
2. **avg_ampの揺らぎ差の再現性確認**：「寝ている」「離れた」をもう1〜2回、できれば3〜5分程度の揃った時間で撮り直し、標準偏差の差（在室/不在の判定に使えそうな差）が再現するか確認する。今回チップ差はほぼないと分かったので、外付けアンテナの市販品（XIAO C3 / M5AtomS3 Lite）で撮り直すのが望ましい
3. **Level 2への移行判断**：現状のLevel 1（セルフセンシング）でAP側由来のノイズが除去できないことが確認できたので、精度をさらに詰めたいなら、専用TXノードとのペア構成（Level 2）を試す価値がある。ノード自体はXIAO C3やM5AtomS3 Liteなど市販品を使う方が良さそうと分かった（車iot機は信号品質面で不利）
4. **Home Assistant連携（MQTT or ESPHome）**：まだ着手しない。シリアル検証で実用に足る精度が確認できてから検討する（ユーザーの明示的な希望）
5. **元々の活動ログ設計への回帰**：センシング方式が固まったら、4カテゴリ（就寝中/作業中/休憩中/外出中）の活動状態をApexCharts-cardのタイムラインで可視化する、という当初のゴールに戻る

## ディレクトリ構成（現状）

```
.
├── README.md                 # プロジェクト概要
├── platformio.ini            # PlatformIO設定（env:esp32-s3, env:xiao-c3 / arduino framework）
├── src/
│   └── main.cpp               # CSI単体検証用ファームウェア（シリアル出力のみ、本命ルート）
├── esphome/                   # ESPHome版（参考・代替ルート、現在未使用）
│   ├── csi_test_atoms3lite.yaml
│   └── csi_test_cariot_devboard.yaml
├── docs/
│   ├── verification_procedure.md   # ESPHome版の検証手順（参考）
│   └── handoff.md                  # このファイル
└── logs/                      # シリアルモニタの生ログ（上記の表を参照）
```
